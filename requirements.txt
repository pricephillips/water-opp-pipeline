requests>=2.31.0
pandas>=2.1.0
numpy>=1.24.0
scipy>=1.11.0
openpyxl>=3.1.0

# Optional — only needed if processing WRI Aqueduct as GeoPackage (.gpkg)
# The pipeline also accepts the flat baseline_annual.csv (recommended; no extra deps)
# geopandas>=0.14.0
# fiona>=1.9.0
